# Workflow and Agent-Proxy Scope

Reproduced: a fixed holdout, standardized metrics, proposal validation,
prompt-injection failures, cost recording, and deterministic reruns.

Not reproduced: any LLM, ReAct prompt/trajectory, AgentBench environment,
model result, real tool execution, or claim about real agent architectures.

Exact command:

```text
paper-repro run --study workflow-agent --output research/results/v0.1
```
